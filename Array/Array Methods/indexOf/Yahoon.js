var num =[1,2,3,4,5,6,5];

// var output=num.indexOf(5);
// console.log(output);


var output2=num.indexOf(5,5);
console.log(output2);




Syntax:

str.indexOf(searchValue , index)


// This function returns the index of the string (0-based) where the search value is found for the first time. If the search value cannot be found in the string then the function returns -1.